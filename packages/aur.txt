# Foreign/AUR packages (yay itself is bootstrapped as yay-bin by setup.sh —
# never list it here; strip -debug split-packages when regenerating).
claude-code
claude-desktop
wezterm-git

# ground-up project toolchain (AUR side)
icestorm                  # iCE40 bitstream tools (icepack/iceprog) — Sprint 3
riscv-gnu-toolchain-bin   # RISC-V cross compiler — Sprint 4-5 (big download)
